import sys  #sys (system) is imported first so that python can be able to assess it
print(sys.version)  #'print' enables python to interpret the command
                   #(sys.vertion) is the command, and it is typed without quotes("").
                   